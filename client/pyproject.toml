[project]
name = "client"
version = "0.1.0"
description = "Client for AI advent challenge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=1.1.11",
    "python-dotenv>=1.2.2",
]
