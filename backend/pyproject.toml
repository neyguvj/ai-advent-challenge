[project]
name = "backend"
version = "0.1.0"
description = "Backend server for AI advent challenge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=2.3.3",
    "gunicorn>=25.1.0",
    "langchain-gigachat>=0.3.12",
    "python-dotenv>=1.2.2",
]
