[project]
name = "ai-advent-challenge"
version = "0.1.0"
description = "AI advent challenge project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backend",
    "cli",   
]

[tool.uv.workspace]
members = [
    "backend", 
    "cli",    
]

[tool.uv.sources]
cli = { workspace = true }
backend = { workspace = true }
