[project]
name = "cli"
version = "0.1.0"
description = "CLI for AI advent challenge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-gigachat>=0.3.12",
    "python-dotenv>=1.2.2",
    "requests>=2.31.0",
]
